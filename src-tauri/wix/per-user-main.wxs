<?if $(sys.BUILDARCH)!="x64"?>
  <?error Copilot Bridge Atlas requires x64?>
<?endif?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
  <Product Id="*" Name="{{product_name}}" UpgradeCode="{{upgrade_code}}"
           Language="!(loc.TauriLanguage)" Manufacturer="{{manufacturer}}" Version="{{version}}">
    <Package Id="*" InstallerVersion="450" Languages="0" Compressed="yes"
             InstallScope="perUser" InstallPrivileges="limited" SummaryCodepage="!(loc.TauriCodepage)"/>
    <MajorUpgrade Schedule="afterInstallInitialize" AllowSameVersionUpgrades="yes"
                  DowngradeErrorMessage="!(loc.DowngradeErrorMessage)"/>
    <Media Id="1" Cabinet="app.cab" EmbedCab="yes"/>
    <Icon Id="ProductIcon" SourceFile="{{icon_path}}"/>
    <Property Id="ARPPRODUCTICON" Value="ProductIcon"/>
    <Property Id="ARPNOREPAIR" Value="yes" Secure="yes"/>
    <SetProperty Id="ARPNOMODIFY" Value="1" After="InstallValidate" Sequence="execute"/>
    <Property Id="AUTOLAUNCHAPP" Secure="yes"/>
    <Property Id="LAUNCHAPPARGS" Secure="yes"/>
    <Property Id="INSTALLDIR">
      <RegistrySearch Id="PreviousInstallDir" Root="HKCU"
                      Key="Software\{{manufacturer}}\{{product_name}}" Name="InstallDir" Type="raw"/>
    </Property>
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT" Value="!(loc.LaunchApp)"/>
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOX" Value="1"/>
    <CustomAction Id="LaunchApplication" Impersonate="yes" FileKey="Path"
                  ExeCommand="[LAUNCHAPPARGS]" Return="asyncNoWait"/>
    <UI>
      <Publish Dialog="ExitDialog" Control="Finish" Event="DoAction" Value="LaunchApplication">
        WIXUI_EXITDIALOGOPTIONALCHECKBOX = 1 and NOT Installed
      </Publish>
      <Property Id="WIXUI_INSTALLDIR" Value="INSTALLDIR"/>
      <Publish Dialog="WelcomeDlg" Control="Next" Event="NewDialog" Value="InstallDirDlg" Order="2">1</Publish>
      <Publish Dialog="InstallDirDlg" Control="Back" Event="NewDialog" Value="WelcomeDlg" Order="2">1</Publish>
    </UI>
    <UIRef Id="WixUI_InstallDir"/>

    <Directory Id="TARGETDIR" Name="SourceDir">
      <Directory Id="DesktopFolder"/>
      <Directory Id="LocalAppDataFolder">
        <Directory Id="UserPrograms" Name="Programs">
          <Directory Id="INSTALLDIR" Name="{{product_name}}"/>
        </Directory>
      </Directory>
      <Directory Id="ProgramMenuFolder">
        <Directory Id="ApplicationProgramsFolder" Name="{{product_name}}"/>
      </Directory>
    </Directory>

    <DirectoryRef Id="INSTALLDIR">
      <Component Id="RegistryEntries" Guid="*" Win64="yes">
        <RegistryValue Root="HKCU" Key="Software\{{manufacturer}}\{{product_name}}"
                       Name="InstallDir" Type="string" Value="[INSTALLDIR]" KeyPath="yes"/>
        <RemoveFolder Id="RemoveInstallDir" Directory="INSTALLDIR" On="uninstall"/>
        <RemoveFolder Id="RemoveEmptyUserPrograms" Directory="UserPrograms" On="uninstall"/>
      </Component>
      <Component Id="Path" Guid="{{path_component_guid}}" Win64="yes">
        <File Id="Path" Source="{{main_binary_path}}" KeyPath="no" Checksum="yes"/>
        <RegistryValue Root="HKCU" Key="Software\{{manufacturer}}\{{product_name}}"
                       Name="PathComponent" Type="integer" Value="1" KeyPath="yes"/>
      </Component>
      <Component Id="License" Guid="60f65090-5827-4dc5-a01c-f399a61a0b1a" Win64="yes">
        <File Id="LicenseFile" Name="LICENSE.rtf" Source="{{license}}" KeyPath="no"/>
        <RegistryValue Root="HKCU" Key="Software\{{manufacturer}}\{{product_name}}"
                       Name="LicenseComponent" Type="integer" Value="1" KeyPath="yes"/>
      </Component>
    </DirectoryRef>

    <DirectoryRef Id="ApplicationProgramsFolder">
      <Component Id="StartMenuShortcut" Guid="*" Win64="yes">
        <Shortcut Id="ApplicationStartMenuShortcut" Name="{{product_name}}"
                  Description="Runs {{product_name}}" Target="[!Path]" Icon="ProductIcon"
                  WorkingDirectory="INSTALLDIR">
          <ShortcutProperty Key="System.AppUserModel.ID" Value="{{bundle_id}}"/>
        </Shortcut>
        <Shortcut Id="UninstallShortcut" Name="Uninstall {{product_name}}"
                  Target="[System64Folder]msiexec.exe" Arguments="/x [ProductCode]"/>
        <RemoveFolder Id="RemoveApplicationProgramsFolder" Directory="ApplicationProgramsFolder" On="uninstall"/>
        <RegistryValue Root="HKCU" Key="Software\{{manufacturer}}\{{product_name}}"
                       Name="StartMenuShortcut" Type="integer" Value="1" KeyPath="yes"/>
      </Component>
    </DirectoryRef>
    <DirectoryRef Id="DesktopFolder">
      <Component Id="DesktopShortcut" Guid="*" Win64="yes">
        <Shortcut Id="ApplicationDesktopShortcut" Name="{{product_name}}"
                  Description="Runs {{product_name}}" Target="[!Path]" Icon="ProductIcon"
                  WorkingDirectory="INSTALLDIR">
          <ShortcutProperty Key="System.AppUserModel.ID" Value="{{bundle_id}}"/>
        </Shortcut>
        <RegistryValue Root="HKCU" Key="Software\{{manufacturer}}\{{product_name}}"
                       Name="DesktopShortcut" Type="integer" Value="1" KeyPath="yes"/>
      </Component>
    </DirectoryRef>

    <Feature Id="MainProgram" Title="Copilot Bridge Atlas" Level="1"
             ConfigurableDirectory="INSTALLDIR" AllowAdvertise="no" Absent="disallow">
      <ComponentRef Id="RegistryEntries"/>
      <ComponentRef Id="Path"/>
      <ComponentRef Id="License"/>
      <ComponentRef Id="StartMenuShortcut"/>
      <ComponentRef Id="DesktopShortcut"/>
    </Feature>

    {{#if install_webview}}
    <Property Id="WVRTINSTALLED">
      <RegistrySearch Id="WVRTInstalledSystem" Root="HKLM" Key="SOFTWARE\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}" Name="pv" Type="raw" Win64="no"/>
      <RegistrySearch Id="WVRTInstalledUser" Root="HKCU" Key="SOFTWARE\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}" Name="pv" Type="raw"/>
    </Property>
    {{#if download_bootstrapper}}
    <CustomAction Id="DownloadAndInvokeBootstrapper" Directory="INSTALLDIR" Execute="deferred" ExeCommand='powershell.exe -NoProfile -WindowStyle Hidden Invoke-WebRequest -Uri "https://go.microsoft.com/fwlink/p/?LinkId=2124703" -OutFile "$env:TEMP\AtlasWebView2Setup.exe"; Start-Process -WindowStyle Hidden -FilePath "$env:TEMP\AtlasWebView2Setup.exe" -ArgumentList ({{webview_installer_args}} &apos;/install&apos;) -Wait' Return="check"/>
    <InstallExecuteSequence>
      <Custom Action="DownloadAndInvokeBootstrapper" Before="InstallFinalize">
        <![CDATA[NOT(REMOVE OR WVRTINSTALLED)]]>
      </Custom>
    </InstallExecuteSequence>
    {{/if}}
    {{#if webview2_bootstrapper_path}}
    <Binary Id="WebView2Bootstrapper" SourceFile="{{webview2_bootstrapper_path}}"/>
    <CustomAction Id="InvokeBootstrapper" BinaryKey="WebView2Bootstrapper" Execute="deferred"
                  ExeCommand="{{webview_installer_args}} /install" Return="check"/>
    <InstallExecuteSequence>
      <Custom Action="InvokeBootstrapper" Before="InstallFinalize">
        <![CDATA[NOT(REMOVE OR WVRTINSTALLED)]]>
      </Custom>
    </InstallExecuteSequence>
    {{/if}}
    {{/if}}
    <InstallExecuteSequence>
      <RemoveShortcuts>Installed AND NOT UPGRADINGPRODUCTCODE</RemoveShortcuts>
      <Custom Action="LaunchApplication" After="InstallFinalize">AUTOLAUNCHAPP AND NOT Installed</Custom>
    </InstallExecuteSequence>
    <SetProperty Id="ARPINSTALLLOCATION" Value="[INSTALLDIR]" After="CostFinalize"/>
  </Product>
</Wix>
